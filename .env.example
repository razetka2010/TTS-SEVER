# Скопируйте файл в ".env" в корне проекта и при необходимости измените значения.
# Файл .env не попадает в Git (см. .gitignore).

# Секрет для экрана TV без входа (минимум 12 символов).
# URL будет вида: https://ваш-сайт/requests/tv/ЭТОТ_СЕКРЕТ/
TV_BOARD_SECRET=
